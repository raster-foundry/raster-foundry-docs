import { SidebarExpandableItemDirective } from './sidebar-expandable-item.directive';

describe('SidebarExpandableItemDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarExpandableItemDirective();
    expect(directive).toBeTruthy();
  });
});
