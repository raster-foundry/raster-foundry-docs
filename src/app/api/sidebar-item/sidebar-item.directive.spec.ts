import { SidebarItemDirective } from './sidebar-item.directive';

describe('SidebarItemDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarItemDirective();
    expect(directive).toBeTruthy();
  });
});
